
describe('Meaningful Test', () => {
    it('1 + 1 => 2', () => {
        expect(1 + 1).toBe(2);
    });
});

describe('main test', () => {
    it('always fails', () => {
        expect(1).toBe(1);
    });
});

